<?php

namespace App\Services\Export;

/**
 * Exporter pour Win Auditor
 * Utilise le même format que le CSV générique
 */
class WinAuditorExporter extends GenericCsvExporter
{
    // Win Auditor utilise le même format CSV que le générique
    // Pas de customisation nécessaire
}
